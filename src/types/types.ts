import { Vector3 } from 'three';

export type TTriangulation = [Vector3, Vector3, Vector3][];
export type TTriangulationResponse = TTriangulationResponseItem[];
export type TTriangulationResponseItem = [number, number, number][];
